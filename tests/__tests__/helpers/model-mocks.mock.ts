import { mockFunctions } from '@digitaldefiance/node-express-suite';

const { makeRoleModel, makeUserModel, makeUserRoleModel } = mockFunctions(jest);

export { makeRoleModel, makeUserModel, makeUserRoleModel };
